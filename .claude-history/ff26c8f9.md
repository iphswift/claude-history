# Conversation since last commit

### User

I want an ability to set the timestamp for "the last commit" to a current timestamp. It should be a command line command based on the tool. So if the tool is called "mytool" it would be "mytool reset" which would leave a mark in the conversation directory that it should not take anything before the timestamp that the method is called on.

### Assistant

Let me explore the current codebase first.

### Assistant

Now I understand the structure. I'll add a `reset` command (`python3 main.py reset` or `--reset`) that writes a timestamp marker to `.claude-history/.reset`, and update the cutoff logic to respect it.

### Assistant

Now update the cutoff logic in `messages_since_last_commit` to factor in the reset timestamp:

### Assistant

Also update `run_hook` to respect the reset timestamp when computing its cutoff:

### Assistant

Now add the CLI entry point:

### Assistant

Let me verify the final state of the relevant sections:

### Assistant

Done. Here's what was added:

**New command:**
```bash
python3 main.py reset
# or
python3 main.py --reset
```

**What it does:**
- Writes the current UTC timestamp to `.claude-history/.reset`
- Any subsequent run (manual or post-commit hook) will use `max(commit_timestamp, reset_timestamp)` as the cutoff — so messages before the reset are silently excluded

**Key changes in `main.py`:**
- `RESET_FILE = ".claude-history/.reset"` — marker file path (`main.py:12`)
- `get_reset_timestamp()` — reads the marker, returns `0.0` if absent (`main.py:64`)
- `run_reset()` — writes current timestamp and prints a confirmation (`main.py:84`)
- `messages_since_last_commit()` — applies `max(cutoff, reset_ts)` after computing commit-based cutoff (`main.py:131`)
- `run_hook()` — same `max()` logic for the hook cutoff (`main.py:264`)
