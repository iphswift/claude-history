# Conversation since last commit

### User

I got the following error on github. Is there a way to test this locally? /opt/hostedtoolcache/Python/3.12.13/x64/bin/python: No module named pytest
Error: Process completed with exit code 1.

### Assistant

Let me look at the GitHub Actions workflow and project structure.

### Assistant

The workflow is missing the `pip install pytest` step. Fix:

### Assistant

To test locally, just run:

```bash
python -m pytest tests/ -v
```

If you don't have pytest installed:

```bash
pip install pytest
python -m pytest tests/ -v
```
