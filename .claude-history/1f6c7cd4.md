# Conversation since last commit

### User

modify the git hook script to ammend the commit (and add the converation text beneath the commit message)

### Assistant

The hook will now amend the commit message with the full conversation text (the same markdown content written to `.claude-history/<hash>.md`) appended directly below the original commit message, instead of just a file path link.

### User

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

### User

<command-name>/exit</command-name>
            <command-message>exit</command-message>
            <command-args></command-args>

### User

<local-command-stdout>Bye!</local-command-stdout>
